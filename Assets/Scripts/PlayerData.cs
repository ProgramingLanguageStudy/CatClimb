using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 정적 클래스
// 객체로 만들지 않고 클래스 자체로 사용을 하겠다.
public static class PlayerData
{
    // 이번 판 최고 높이 기록
    public static float HeightRecord = 0.0f;

    // 역대 최고 높이 기록
    public static float BestHeightRecord = 0.0f;
}
